import * as React from "react";
import * as Types from "./webflow_modules/types";

declare function ContactFormCta(
    props: {
        as?: React.ElementType;
        text1?: React.ReactNode;
        text2?: React.ReactNode;
    }
): React.JSX.Element